version: 1
frontend:
  phases:
    preBuild:
      commands: []
    build:
      commands:
        - echo "No build step for static site"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []